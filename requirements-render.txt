# Lean dependency set for the Render free tier (512 MB RAM).
# No PyTorch / sentence-transformers: the deployed app uses
# EMBEDDINGS_BACKEND=gemini (Google's embedding API) instead of the
# local HuggingFace model. Local development uses requirements.txt.

fastapi
uvicorn[standard]
python-multipart
python-dotenv

langchain
langchain-community
langchain-google-genai
langchain-groq

pypdf
faiss-cpu

# Screen understanding (Agent 4). pytesseract is tiny; without the
# Tesseract binary on the host the app simply skips the OCR fallback.
pillow
pytesseract

# Automation & integrations (Agent 6)
fpdf2

# Persistent memory
sqlalchemy
psycopg2-binary
